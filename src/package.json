{
  "name": "src",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-local": "functions-framework --target=imageResize",
    "start-dev": "nodemon --exec npm run start-local"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/functions-framework": "^3.0.0",
    "@google-cloud/storage": "^5.18.2",
    "sharp": "^0.30.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
